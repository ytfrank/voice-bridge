# 提测报告 — voice-bridge V1.5.3 Hotfix

**提测时间**：2026-03-31 21:40  
**提测人**：Peter  
**分支**：hotfix/v1.5.3-exp-direct-recovery  
**最新 Commit**：`b052e17`  

---

## 一、本轮目标

V1.5.2 线上确认后，波哥要求**回退到短期最短链路方案**：

> 直接提供一个可在 Safari 中打开的 `exp://...` 地址，拉起 Expo Go。

因此，V1.5.3 的目标不是继续扩展复杂中转页，而是：

1. 恢复到最短、最直接的 Expo Go 拉起方式
2. 将当前实际可用的 `exp://...` 地址作为线上确认入口
3. 先恢复可用性，再在后续复盘中处理长期稳定入口方案

---

## 二、当前确认结果

从当前运行中的 Expo manifest 读取到：

- **hostUri**：`aswx_oy-ytfrank-8082.exp.direct`
- **exp 直链**：`exp://aswx_oy-ytfrank-8082.exp.direct`

该地址来源于当前本机 Expo tunnel（8082），为当前运行中的实际地址。

---

## 三、本轮 Hotfix 结论

### 本轮是否有新的应用代码改动？
**没有新的业务代码改动。**

V1.5.3 本质上是：
- 回退到“直接使用 `exp://...` 直链”的短期策略
- 由部署侧/运行态给出当前直链
- 用最短链路恢复线上确认能力

因此，这一轮主要成果物是：
- 明确当前可用的 exp 直链
- 通过 hotfix 流程留痕
- 交 Guard 做快速确认

---

## 四、Guard 建议测试范围

由于 V1.5.3 不涉及新的业务代码改动，建议 Guard 按**小 hotfix 快速确认**处理：

1. 确认当前 exp 直链已从运行中的 Expo 实例读取，而不是历史旧地址
2. 确认当前直链格式正确：
   - `exp://aswx_oy-ytfrank-8082.exp.direct`
3. 如线下/流程侧确认无误，可**可控放行**
4. 最终由波哥在线上做 Safari → Expo Go 实际确认

---

## 五、当前用于线上确认的直链

**请以当前这条为准：**

`exp://aswx_oy-ytfrank-8082.exp.direct`

---

## 六、后续说明

1. 若该直链本身验证通过，则 V1.5.3 即完成短期恢复目标
2. 若该直链仍失败，则问题将被明确收敛为：
   - Expo tunnel host 形态 / Safari deep link 兼容问题
   - 属于部署入口层问题，不再是本轮代码 hotfix 范围
3. 后续长期方案仍建议由 Peter + Atlas 继续推进稳定入口方案（固定域名 / Named Tunnel / 自动验证）

---

*Peter · 2026-03-31*
